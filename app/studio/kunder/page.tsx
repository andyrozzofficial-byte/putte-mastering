import { PlaceholderPage } from "../placeholder-page";

export const dynamic = "force-dynamic";

export default function CustomersPage() {
  return (
    <PlaceholderPage
      title="Customers"
      description="Customer profiles will appear automatically after the first order."
    />
  );
}
